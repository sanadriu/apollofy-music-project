import styled from "styled-components";

const HomeSmallText = styled.p`
  font-size: 0.8rem;
  font-weight: 500;
  margin: 0;
  color: ${({ theme }) => theme.colors.text};
`;

export default HomeSmallText;
