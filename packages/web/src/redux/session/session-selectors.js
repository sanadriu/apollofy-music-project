export const selectUserAgent = (state) => state.session.userAgent;

export const selectCoordinates = (state) => state.session.coordinates;

export const selectSessionState = (state) => state.session;
